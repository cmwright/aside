{
  "name": "voice-to-text-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that turns dictated audio into clean text (STT + LLM cleanup + user dictionary).",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "smoke": "./scripts/smoke.sh"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260908.1",
    "typescript": "5.9.3",
    "vitest": "3.2.4",
    "wrangler": "4.130.0"
  }
}
